'use server';
// lib/actions.ts — Server actions (Firebase auth handles sign-in/out client-side)
// This file is kept for any future server actions (e.g., form submissions, data mutations)

export async function placeholder() {
  // placeholder — add server actions here as needed
}
